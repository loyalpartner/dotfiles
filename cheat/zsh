# 快捷键使用

### 使用场景

> mkdir foo
> cd foo 
> touch foo bar fill

如果使用搜索命令,每条命令都搜索会很麻烦
(accept-line-and-down-history)会先从历史记录找到
第一条需要的命令，按下，命令之行后，历史记录中的
下一条命令就会出现，可以持续按直到最后一条命令

### ^[q,^[g使用场景

有时候想用cd somedir，可是目录不存在
这时候可以^[q将命令入栈，mkdir somedir,然后命令
cd somedir 会自动弹出
若是命令入栈后发现原来somedir目录存在，不需要
创建目录，此时可以使用^[g将命令拉回来

# 匹配括号  
# 查找字符  
# Redo      
# 扩展Alias a
# 更正文件名C
# 更正命令单词  C
# Quote Region  ^[\"
# Quote Line    ^['
# 插入!$        ^[.
# 重复输入字符  ^[+数字
# 帮助          ^[H
# 大写字符,单词      ^[C,^[U
# ls^[p ^[n   相当于!ls
# 替换前后单词  ^[T
# 复制上个单词  ^[m
# ^[a,^[g,^[|
