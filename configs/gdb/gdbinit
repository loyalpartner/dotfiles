# GDB configuration

#######################
#  Settings           #
#######################

set debuginfod enabled off

# Skip noisy library functions
skip -rfu '^(absl|std|std::Cr)(::[a-zA-z0-9_]+)+'
skip -rfu '([a-zA-z0-9_]+)::\1'
skip -rfu 'operator.*'
skip -gfi 'base/allocator/*.h'

# x86 disassembly flavor
set disassembly-flavor intel

# History
set pagination off
set history save on
set history size 10000
set history remove-duplicates unlimited
set history filename ~/.gdb_history

# Pretty-printing
set print pretty on
set print object on
set print array on
set print array-indexes on
set print static-members on
set print vtbl on
set print demangle on
set print asm-demangle on
set listsize 20

# Prompt
set prompt \033[01;31m(gdb) \033[0m

#######################
#  Custom Commands    #
#######################

# Print values in hex
define px
    set $i = 0
    while $i < $argc
        eval "p/x $arg%d", $i
        set $i = $i + 1
    end
end
document px
Print values in hexadecimal format.
Usage: px expr1 expr2 ...
end

# Print a C++ string
define ps
    print $arg0.c_str()
end
document ps
Print a C++ string using c_str().
end

# Print a C++ wstring
define pws
    printf "\""
    set $c = (wchar_t*)$arg0
    while ( *$c )
        if ( *$c > 0x7f )
            printf "[%x]", *$c
        else
            printf "%c", *$c
        end
        set $c++
    end
    printf "\"\n"
end
document pws
Print a C++ wstring or wchar_t*.
end

# Stack trace with assertion scopes
define mybt
python
import re
frame_re = re.compile(r"^#(\d+)\s*(?:0x[a-f\d]+ in )?(\w*(::\w+|::\([\w\s]+\))*)\(")
btl = gdb.execute("backtrace full", to_string=True).splitlines()
for line in btl:
    m = frame_re.match(line)
    if m:
        print("%-60s" % m.group(2))
end
end
document mybt
Print stack trace with assertion scopes.
end

#######################
#  Optional Modules   #
#######################
# Uncomment any line to enable.

# source ~/dotfiles/configs/gdb/break_history.py    # save/load breakpoints per binary
# source ~/dotfiles/configs/gdb/editor_manager.py   # vim integration
# source ~/dotfiles/configs/gdb/qt5.gdb             # Qt5 pretty-printers

#######################
#  Local Override     #
#######################

python
from os.path import exists, expandvars
local = expandvars('$HOME/.gdb.local')
if exists(local):
    gdb.execute(f'source {local}')
end
